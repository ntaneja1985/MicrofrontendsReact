### Example user template template
### Example user template

# IntelliJ project files
.idea
*.iml
out
gen

# ---------------------------------------------------
# Global ignores for a monorepo with multiple JS projects
# ---------------------------------------------------

# Node.js dependencies
# This rule will ignore `node_modules` folders in any directory,
# including the root and all subfolders.
**/node_modules

# Build and distribution directories
# Ignores common build output folders like `build`, `dist`, `out`, and `coverage`.
# The `**/` pattern ensures this applies to all subfolders.
**/build
**/dist
**/out
**/coverage

# Logs
# Ignores various log files generated by npm, yarn, or other tools.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
# This ignores all `.env` files, which may contain sensitive information.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and OS files
# Ignores files created by popular IDEs and operating systems.
# The `.idea/` folder is from WebStorm/IntelliJ, `.vscode/` from VS Code.
.idea/
.vscode/
.DS_Store
Thumbs.db

# Other common files to ignore
*.log
*.log.*
*.iml
.eslintcache
.cache

# Yarn v2+
# Ignores the `.yarn/cache` folder which can get very large.
.yarn/cache
.pnp.*

# Visual Studio Code local workspace file
.vscode/settings.json
.vscode/tasks.json
.vscode/launch.json
.vscode/extensions.json
.vscode/compile.json